# GLFW bindings

The bindings are made manually to be more ergonomic.

## Compiling
1. Clone GLFW into this directory
```bash
git clone git@github.com:glfw/glfw.git
```
2. Make sure the resulting directory is named `glfw`, if not, rename it.
3. Run the build metaprogram
```bash
jai ./build.jai
```
